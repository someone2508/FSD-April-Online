// arr.push(6);
// arr.push(7);

// console.log(arr);

// arr.pop();
// arr.pop();

// console.log(arr);

// let arr = [1, 2, 3, 4, 5];

// while (arr.length > 0) {
//   let lastElem = arr.pop();
//   console.log(lastElem); // 5, 4, 3, 2, 1
// }

// console.log("All elements removed!");
// console.log(arr);

// let arr = [1, 2, 3, 4, 5];

// arr.pop(); // removes the last element of my array

// arr.shift(); // remove the element from the begining of the array

// arr.unshift(10);

// console.log(arr);

// let arr = [1, 2, 3, 4, 5];

// const subArray = arr.slice(1, 4); // 1 is included, 4 is excluded.

// console.log(subArray);

// how to create a copy of an array using slice.

let arr = [1, 2, 3, 4, 5];

let copy = arr.slice(0, arr.length);

console.log(copy);
