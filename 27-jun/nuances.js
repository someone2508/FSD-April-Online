console.log(10 + 20);

// when you add 2 strings, they concatinate/join
console.log("10" + "20");

// auto type casting
// rule: when one side of the value is a string,
// it automatically, type casts the other type as a string.
console.log("10" + 20);

console.log(10 + "20");
